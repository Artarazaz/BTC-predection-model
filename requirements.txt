# requirements.txt

# Core data handling & analysis
pandas>=2.0.0
numpy>=1.24.0

# Visualization (optional but useful for exploration)
matplotlib>=3.7.0
seaborn>=0.12.0

# Technical indicators (highly recommended for crypto/time-series)
pandas_ta>=0.3.14b0          # or ta-lib if you have it compiled

# Machine Learning / Modeling basics
scikit-learn>=1.2.0
xgboost>=2.0.0
lightgbm>=4.0.0              # optional, good alternative to xgboost

# Time-series specific (optional but great for forecasting)
statsmodels>=0.14.0
prophet>=1.1.0               # Facebook Prophet, easy to use

# Jupyter & interactive work
jupyter>=1.0.0
notebook>=7.0.0
ipywidgets>=8.0.0            # for interactive plots if needed

# Optional: for fetching live data or more advanced stuff
ccxt>=4.0.0                  # to fetch live Binance data or extend dataset
requests>=2.28.0             # if you add news APIs later

# Optional: faster parquet reading/writing (recommended for large files)
pyarrow>=12.0.0